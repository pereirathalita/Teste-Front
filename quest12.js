// 12) const a = [1, 2, 3, 4, 5]  
// Crie uma nova matriz que multiplica cada elemento por 2, utilizando map ou forEach 

const a = [1, 2, 3, 4, 5]

const multiplicacao = a.map( a => a * 2 )

console.log(multiplicacao)