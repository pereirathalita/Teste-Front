// 5) Qual é a diferença entre classes e IDs em CSS? 

// Representada pelo símbolo de ponto (.) as classes podem ser reutilizadas. 
// Os IDs, não podem ser reutilizados e são representados através do símbolo da cerquilha (#). 